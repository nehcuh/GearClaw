# Memory (记忆) 系统使用指南

GearClaw 的 Memory 系统提供了基于向量搜索的长期记忆功能，可以让 Agent 记住和检索相关信息。

## 功能概述

Memory 系统通过以下方式工作：

1. **索引**：扫描 workspace 中的 Markdown 文件
2. **向量化**：使用 LLM embeddings 将文本转换为向量
3. **存储**：将向量和文本块存储在 SQLite 数据库中
4. **检索**：使用余弦相似度搜索相关内容
5. **集成**：自动将相关记忆添加到对话上下文中

## 配置

Memory 系统通过 `~/.gearclaw/config.toml` 配置：

```toml
[agent]
workspace = "/Users/huchen/Projects/GearClaw"  # 要索引的目录
memory_enabled = true                           # 启用/禁用记忆功能

[memory]
enabled = true                                   # 启用记忆索引
db_path = "~/.gearclaw/memory.db"               # SQLite 数据库路径
```

## 数据结构

Memory 使用 SQLite 数据库存储：

```sql
-- 文件表：记录已索引的文件
CREATE TABLE files (
    path TEXT PRIMARY KEY,
    source TEXT NOT NULL,
    hash TEXT NOT NULL,
    mtime INTEGER NOT NULL,
    size INTEGER NOT NULL
);

-- 文本块表：存储分块的文本和向量
CREATE TABLE chunks (
    id TEXT PRIMARY KEY,
    path TEXT NOT NULL,
    source TEXT NOT NULL,
    text TEXT NOT NULL,
    embedding TEXT NOT NULL,
    start_line INTEGER,
    end_line INTEGER
);

-- 元数据表
CREATE TABLE meta (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL
);
```

## 使用方法

### 1. 自动索引（推荐）

Memory 系统在 Agent 初始化时**自动同步**：

```bash
# 启动 Agent（会自动索引）
gearclaw chat

# 或启动 Gateway
gearclaw gateway
```

启动时会看到：
```
INFO Memory is enabled, starting initial sync...
INFO 开始同步记忆...
INFO Indexing file: README.md
INFO Indexing file: docs/guide.md
...
INFO Memory sync completed.
```

### 2. 手动同步

使用 CLI 命令手动触发同步：

```bash
# 同步记忆索引
gearclaw memory sync
```

**何时需要手动同步：**
- 添加了大量新文档
- 修改了现有文档
- 首次设置记忆系统

### 3. 搜索记忆

测试记忆搜索功能：

```bash
# 搜索相关内容
gearclaw memory search "如何使用 Agent"

# 输出示例：
# 🔍 搜索结果:
# 1. [0.89] docs/agent.md (Line 45)
#    Agent 是一个基于 LLM 的智能助手，可以...
# 2. [0.76] README.md (Line 12)
#    使用 Agent 时，首先需要配置 API key...
# 3. [0.65] docs/setup.md (Line 8)
#    Agent 的配置文件位于 ~/.gearclaw/config.toml...
```

### 4. 在对话中自动使用

当 `memory_enabled = true` 时，Agent 会在每次对话时：

1. 自动搜索与用户消息相关的记忆
2. 将最相关的 3 条记忆添加到 system prompt
3. 基于这些上下文生成更准确的回答

**System Prompt 示例：**
```
You are GearClaw, an AI assistant...

=== Relevant Context ===
The following information from your memory may be relevant to this conversation:

- [docs/api.md] The API endpoint for creating users is POST /api/users (score: 0.92)
- [docs/auth.md] Authentication requires a Bearer token in the Authorization header (score: 0.87)
- [docs/errors.md] Common error codes: 400 (Bad Request), 401 (Unauthorized) (score: 0.73)
========================

User: How do I create a new user?
```

## 工作流程

```
┌─────────────────────────────────────────────────────────┐
│  1. 文件扫描 (Workspace)                                │
│     ~/.gearclaw/workspace/**/*.md                       │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  2. 文本分块                                            │
│     按段落分割 (double newline)                         │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  3. 向量化                                              │
│     LLM Embedding API → Vec<f32>                       │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  4. 存储                                                │
│     SQLite Database (memory.db)                        │
└─────────────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  5. 检索 (用户查询时)                                   │
│     Query → Embedding → Cosine Similarity → Top K       │
└─────────────────────────────────────────────────────────┘
```

## 最佳实践

### 1. Workspace 组织

将相关文档放在 workspace 目录中：

```
~/.gearclaw/workspace/
├── docs/
│   ├── api.md
│   ├── setup.md
│   └── troubleshooting.md
├── README.md
└── notes/
    └── project-notes.md
```

### 2. 文档格式

使用清晰的 Markdown 格式：

```markdown
# API 认证

所有 API 请求都需要 Bearer token 认证。

## 获取 Token

1. 登录系统
2. 访问 /settings/tokens
3. 点击 "Generate Token"

## 使用 Token

在请求头中添加：
```
Authorization: Bearer YOUR_TOKEN
```
```

**技巧：**
- 使用描述性标题
- 按主题分段（使用空行）
- 包含关键词以便搜索
- 使用代码块和示例

### 3. 性能优化

**增量同步**：
- 只索引修改过的文件（基于 mtime）
- 删除已删除的文件记录

**搜索优化**：
- 默认返回 Top 3 最相关的结果
- 可调整 `memory_manager.search(query, k)` 中的 k 值

**数据库维护**：
```bash
# 重建索引（删除数据库后重新同步）
rm ~/.gearclaw/memory.db
gearclaw memory sync
```

### 4. 调试

启用 debug 日志查看详细信息：

```bash
RUST_LOG=debug gearclaw chat

# 会看到：
# DEBUG Found 3 relevant memories
# DEBUG Memory search completed in 123ms
```

## 故障排查

### 问题：没有找到相关记忆

**检查：**
1. 文档是否在 workspace 目录中？
2. 文档是否是 `.md` 格式？
3. 是否已运行 `gearclaw memory sync`？
4. 搜索关键词是否在文档中？

**解决：**
```bash
# 手动触发同步
gearclaw memory sync

# 测试搜索
gearclaw memory search "your keyword"

# 查看数据库
sqlite3 ~/.gearclaw/memory.db
> SELECT COUNT(*) FROM chunks;
> SELECT path FROM chunks LIMIT 10;
```

### 问题：记忆结果不相关

**可能原因：**
- 文档内容质量低
- 搜索查询不够具体
- 需要调整 chunk 大小

**解决：**
1. 改进文档质量，使用清晰的语言
2. 使用更具体的搜索词
3. 考虑添加更多相关文档

### 问题：同步失败

**错误示例：**
```
Error: Failed to get embedding: API request failed
```

**解决：**
1. 检查 API key 配置
2. 确认 embedding 模型可用
3. 检查网络连接

### 问题：内存占用过高

**原因：**
- 大量文档被索引
- Embedding 向量占用内存

**解决：**
- 减少 workspace 中的文档数量
- 使用更小的 embedding 模型
- 定期清理不需要的文档

## 配置示例

### 基础配置

```toml
[agent]
workspace = "~/.gearclaw/workspace"
memory_enabled = true

[memory]
enabled = true
db_path = "~/.gearclaw/memory.db"
```

### 高级配置

```toml
[agent]
name = "MyAgent"
workspace = "/Users/huchen/Projects/MyProject/docs"
memory_enabled = true

[memory]
enabled = true
db_path = "/custom/path/memory.db"
```

## 集成到 Gateway

在 Gateway 模式下，记忆系统同样工作：

```bash
# 启动 Gateway（会自动同步记忆）
gearclaw gateway

# 发送消息到 Discord
@agent 帮我查一下 API 文档中的认证方法

# Agent 会自动：
# 1. 搜索记忆中的相关内容
# 2. 基于记忆上下文回答
# 3. 发送回复到 Discord
```

## API 参考

### MemoryManager

```rust
use gearclaw_core::memory::MemoryManager;

// 创建（通常不需要手动创建）
let manager = MemoryManager::new(config, workspace_path, llm_client)?;

// 同步索引
manager.sync().await?;

// 搜索
let results = manager.search("query string", 5).await?;
for result in results {
    println!("{} (score: {:.2})", result.path, result.score);
    println!("  {}", result.text);
}
```

### SearchResult

```rust
pub struct SearchResult {
    pub path: String,        // 文件路径
    pub text: String,        // 文本内容
    pub score: f32,          // 相似度分数 (0-1)
    pub start_line: Option<usize>,  // 起始行号
}
```

## 相关文档

- [Agent 配置](./config.md)
- [Session 管理](./SESSION.md)
- [CLI 命令参考](./cli.md)

## 常见问题

**Q: Memory 支持哪些文件格式？**

A: 目前只支持 `.md` (Markdown) 文件。计划未来支持更多格式。

**Q: 如何更新已索引的文档？**

A: 只需修改文件，然后运行 `gearclaw memory sync`。系统会检测到修改并重新索引。

**Q: Memory 数据库可以安全删除吗？**

A: 可以。删除后下次同步会重新创建数据库并索引所有文件。

**Q: 搜索速度如何？**

A: 对于数千个文档块，搜索通常在 100-300ms 内完成。性能取决于数据库大小。

**Q: 记忆系统会消耗 API 配额吗？**

A: 只在同步时调用 embedding API。搜索时不消耗 API 配额。

**Q: 可以禁用记忆系统吗？**

A: 可以。设置 `memory_enabled = false` 或 `memory.enabled = false`。

## 示例场景

### 场景 1：技术文档助手

```bash
# 1. 将技术文档放入 workspace
cp -r ~/projects/api-docs/*.md ~/.gearclaw/workspace/docs/

# 2. 同步记忆
gearclaw memory sync

# 3. 使用
gearclaw chat
> 如何重置用户密码？
> [Agent 会从记忆中查找并回答]
```

### 场景 2：项目知识库

```bash
# 1. 添加项目文档
echo "# 项目规范..." > ~/.gearclaw/workspace/specs.md
gearclaw memory sync

# 2. 在 Gateway 中使用
# Discord: @agent 项目的代码规范是什么？
# [自动从记忆中检索规范内容]
```

### 场景 3：学习笔记助手

```bash
# 记录学习笔记
cat > ~/.gearclaw/workspace/study-notes.md <<EOF
# Rust 所有权规则

1. 每个值都有一个所有者
2. 同一时间只能有一个所有者
3. 所有者离开作用域时值被丢弃
EOF

gearclaw memory sync

# 复习时询问
gearclaw chat
> Rust 的所有权规则是什么？
> [Agent 基于笔记回答]
```

## 总结

Memory 系统提供了一个简单但强大的方式让 Agent "记住"信息：

✅ **自动索引** - 启动时自动同步
✅ **智能检索** - 基于向量相似度搜索
✅ **无缝集成** - 自动添加到对话上下文
✅ **增量更新** - 只索引修改过的文件
✅ **易于使用** - CLI 命令简单直观

开始使用：`gearclaw memory sync`
